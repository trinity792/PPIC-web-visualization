/**
 * ComparisonSection.js — REMOVED.
 *
 * Split three ways in phase 8 of the module workbench overhaul:
 *   - the transform radios and base year became `sections/TransformSection.js`
 *   - Top/Bottom N and the value ordering list became `sections/categoryControls.js`,
 *     rendered by `GeographySection` for place charts and `CategoriesSection` otherwise
 *   - the benchmark label input moved into `TransformSection`
 *
 * See `.trash/ComparisonSection.md`. Delete this placeholder. Nothing imports it.
 */

export {};
