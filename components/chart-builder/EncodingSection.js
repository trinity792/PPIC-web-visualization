/**
 * EncodingSection.js — REMOVED.
 *
 * Replaced by `components/chart-builder/sections/AxisSection.js` in phase 7 of
 * the module workbench overhaul; `roleLabel` and `bindableFields` are exported
 * from there now. See `.trash/EncodingSection.md` for what moved and what
 * changed. Delete this placeholder. Nothing imports it.
 */

export {};
