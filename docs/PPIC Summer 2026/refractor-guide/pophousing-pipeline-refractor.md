---
Topic: Population and Housing
Content Type: refractor guide
pinned: false
description: "Reference guide to the refactored Population and Housing pipeline: what it produces, how each phase works, and a per-script breakdown of the modern code with its lineage from the legacy tool. First half is for curious non-programmers; second half is for programmers."
Date Published: June 22, 2026
Last Updated: 07/11/2026 - 10:15 PM
Status: Finalized
Footnote: Document generated by Claude Opus 4.8. Outlined and verified by Trinity Jones.
---

# Population and Housing Refractor Guide

The Population and Housing module turns two families of California Department of Finance (DoF) housing reports into a single, validated, wide CSV of population and housing estimates for every California city, town, county, region, and the state as a whole, from 1991 to the present. This document describes the module in its refactored form: the shared and Population-and-Housing-specific scripts that replaced the legacy monolith, organized around the six phases the pipeline runs.

> [!info] Who this document is for and how to read it
> This document has two halves. The first half - through "Architecture and Boundaries" - is written for someone who wants to understand what the module does and how it works without reading code: the data sources, the phases, the calculations, and the outputs. The second half is a per-script, per-function technical reference for a programmer who wants to know what each piece does and what legacy code it was derived from, faster than reading the source line by line. Nothing here modifies code; a running list of subtle issues found while documenting the module is collected under "Flagged Issues and Fragilities."

---

## What the Module Produces

The pipeline emits one canonical dataset, `PopHousing_Current.csv`, written to the cleaned-data area under `housing-population/`. Each row is one geographic place in one year, carrying population counts, a housing-unit breakdown, occupancy, and two derived rates. The dataset spans five geographic levels - City, Town, County, Region, and State - and every year from 1991 through the latest DoF release, with San Francisco represented at both the City and County levels because DoF treats it as both.

The dataset is "wide" in the sense that all of a place-year's attributes live in one row rather than being melted into a long tidy frame. The frontend visualizations for this module read this CSV directly, so the pipeline's job is to guarantee that the schema, geography, and year coverage are always complete and internally consistent before the file is overwritten.

## Data Sources

The module draws on two DoF report families, both discovered by scraping the DoF estimates landing page rather than by hard-coding file URLs that change every year.

| Source | DoF report | Coverage | Role |
|---|---|---|---|
| Modern estimates | E-5 Population and Housing Estimates | Current vintage, roughly 2020 to present | The live, refreshed portion of the dataset. |
| Historical estimates | E-8 Historical Population and Housing Estimates | 1990 to 2020, in three decade workbooks | The deep-history baseline, built once and then reused. |

The E-5 workbook is an Excel file whose second worksheet holds the data. The E-8 workbooks come in three different column layouts across the three decades they cover, which is the single biggest source of complexity in the historical side of the module.

> [!warning] DoF changes the E-5 format between annual releases - check these first when a run breaks
> The first real end-to-end run against the live 2020-2026 E-5 (2026-07-11) turned up two DoF-side format changes that had nothing to do with the module's logic, and both are the kind of thing likely to recur each year:
>
> - **Workbook filename.** DoF renamed the file from `E-5-2025_Geo_InternetVersion.xlsx` to `E-5_2026_Geo_InternetVersion.xlsx` - a hyphen after `E-5` became an underscore. The discovery and cache patterns now accept either form (`E-5[-_]\d{4}_Geo_InternetVersion\.xlsx`), so a future flip back would still match. If acquisition fails with "No current E-5 workbook could be acquired," confirm the served filename still fits this pattern.
> - **Per-county subtotal labels.** DoF now labels the per-county residual rows as bare `Incorporated` (incorporated-cities subtotal) and `Unincorporated` (unincorporated balance) instead of the older `Balance of <County>`. Neither is a real place, so both are dropped as summary rows (`summary_patterns`). If Phase 3 fails with a burst of duplicate geographic keys, look for a new subtotal label DoF has introduced that the summary filter does not yet catch.

A third input is the module's own committed historical baseline. The E-8 history is materialized once into an immutable CSV (`PopHousing_Historical_E8.csv`) by a separate Phase 0 builder, and the main pipeline reads that file back as its pre-2020 history on every run without ever writing it. Earlier revisions pointed the historical path at the pipeline's own output, creating a self-perpetuating history; that has since been decoupled (see "Flagged Issues and Fragilities," A1).

## The Dataset: Grain, Columns, and Geography

The grain of the output is one row per `(Geographic Level, Location, Year)`. That triple is the key the pipeline deduplicates on at every merge point, and the key the final validator enforces uniqueness against.

The output schema is fixed by `config/schemas.py` and applied identically to historical and modern rows so the two can be concatenated. The columns fall into four groups.

| Group | Columns |
|---|---|
| Identity | `Geographic Level`, `Location`, `Year`, `Source` |
| Population | `Total Population`, `Household Population`, `Group Quarters Population` |
| Housing units | `Total Housing Units`, `Single Family Units`, `Multiple Family Units`, `Mobile Homes`, `Occupied Units`, `Vacant Units`, and the four raw DoF sub-columns (`Single Family Detached Units`, `Single Family Attached Units`, `Two to Four Family Units`, `Five Plus Family Units`) |
| Derived rates | `Vacancy Rate (%)`, `Persons Per Household` |

Geography is where the module carries the most domain knowledge. DoF workbooks list places hierarchically: a county header, then that county's cities and towns, then a "County Total" summary, with a "State Total" at the end. The pipeline has to walk that hierarchy, attach each city to its parent county, promote "County Total" and "State Total" rows into real County and State rows, classify each remaining place as City or Town, and finally roll counties up into the nine custom PPIC regions and the state. San Francisco is deliberately duplicated into a City row and a County row, and this happens exactly once, late in the pipeline, so it is never double-counted.

## How the Pipeline Runs: Six Phases

The orchestrator `scripts/orchestrators/pophousing_pipeline.py` runs six phases in sequence, wrapping each in error handling that reports which phase failed. Each phase hands a DataFrame to the next. The historical E-8 baseline is a prerequisite built by a separate driver (described as "Phase 0" in the technical half); the six live phases below assume that baseline CSV already exists.

### Phase 1: Setup and Validation

The pipeline first loads its four configuration bundles (paths, sources, schemas, geography), then does housekeeping and a safety check. It archives E-5 workbooks older than the 90-day cache window from the download directory and writes advance "this file will be deleted" warnings at 15, 10, 5, and 1 days out. It then confirms the immutable historical baseline exists (aborting with an actionable "run the Phase 0 builder" message if it does not), logs a freshness check comparing the baseline's coverage against the configured E-8 eras, and runs a battery of checks on the baseline - required columns, year coverage back to 1991, presence of State and County and City levels, a plausible California population range, null counts, and duplicate keys. If the historical baseline is missing or malformed, the whole run aborts before any scraping happens, so a bad baseline cannot silently corrupt the output.

### Phase 2: Data Acquisition

The pipeline discovers and downloads the current E-5 workbook. It scrapes the DoF estimates page for the E-5 report heading, follows either a direct workbook link or a year-range landing page to reach the `.xlsx`, and downloads it. A local cache avoids re-downloading a workbook that is still fresh. This phase is written defensively: if discovery fails, or the download fails, the pipeline falls back to the most recent workbook already sitting in the download directory that is still within the fallback age limit. Only if no workbook can be obtained at all does the phase raise.

### Phase 3: E-5 Cleaning

The raw E-5 worksheet becomes clean, canonical rows. This is the busiest phase. The pipeline names the raw positional columns, trims the metadata rows above the first real county (anchored on "Alameda"), renames DoF's `Region`/`City` columns to the pipeline's `County`/`Location`, forward-fills the county down each block, strips summary and header rows while protecting "County Total" and "State Total," fills blank location labels down the hierarchy, parses the year, filters to the modern years, coerces the numeric columns, derives the housing-unit totals, classifies each row's geographic level using California geography, standardizes city and town names, and finally validates the result. If the cleaned frame fails validation, the phase raises rather than passing malformed data downstream.

### Phase 4: Merge Historical and Modern

The pre-2020 history and the freshly cleaned modern data are combined. The pipeline loads the historical CSV, labels it as `E-8`, filters it to years at or before 2020, checks that its schema matches the modern frame exactly, concatenates the two, and resolves any overlapping `(Location, Level, Year)` keys by an explicit source priority that prefers `E-5` over `E-8`. The result is one continuous frame from 1991 to the present with no duplicate place-years.

### Phase 5: Enrichment

The merged county-and-place data gains its regional and state rollups, and its rates are sanity-corrected. The pipeline rebuilds all nine regions by summing their member counties, fills in any California state rows for years the merged data lacks by summing all counties, and recomputes `Vacancy Rate (%)` and `Persons Per Household` for those aggregated rows from the summed components rather than averaging child rates. It then detects vacancy rates that were accidentally stored as decimal fractions (for example 0.07 instead of 7.0) and scales them to percentages, and validates that every rate now falls in a plausible range.

### Phase 6: Finalize and Output

The enriched frame is normalized one last time, validated in full, and written. The pipeline asserts every row already carries a geographic level (failing loudly if any does not, rather than re-running a classifier that no longer has county context), standardizes city and town names once more, duplicates San Francisco into City and County rows, enforces the exact output column order and types - preserving each row's `E-5` / `E-8` / `Aggregated` provenance rather than overwriting it - and runs the comprehensive final validator (schema, duplicate keys, valid and required levels, known `Source` values, a California state row, a complete year span, the San Francisco City-and-County invariant, and a Bay Area 2020 population sanity check). Only if all of that passes does it archive the previous `PopHousing_Current.csv` and atomically write the new one, returning a summary of row count, year range, per-level counts, and any acquisition-fallback flags.

## Key Calculations

The module computes a small number of values rather than passing DoF's numbers through unchanged.

| Value | Definition |
|---|---|
| `Single Family Units` | `Single Family Detached Units` + `Single Family Attached Units`. |
| `Multiple Family Units` | `Two to Four Family Units` + `Five Plus Family Units`. |
| `Vacant Units` | `Total Housing Units` - `Occupied Units`. |
| `Vacancy Rate (%)` | `Vacant Units` / `Total Housing Units` x 100, or 0 when there are no units. |
| `Persons Per Household` | `Household Population` / `Occupied Units`, or 0 when there are no occupied units. |

The two rates are recomputed - never averaged - whenever a Region or State row is built, because the correct rate for an aggregate comes from its summed numerator and denominator, not from the mean of its children's rates. The decimal-fraction correction exists because some DoF vintages express the vacancy rate as a fraction between 0 and 1; the pipeline decides this per vintage - if a given year's vacancy rates are mostly below 1.0 it treats the whole year as fraction-encoded and multiplies its sub-1.0 rates by 100 - so a genuinely tiny sub-1% rate in a normal year is never mistakenly rescaled.

## Architecture and Boundaries

The refactor's organizing rule is a hard separation between project-independent mechanisms and Population-and-Housing domain knowledge.

- `scripts/shared/` holds generic, dataset-agnostic mechanisms - HTTP downloads, file retention, type coercion, row filtering, DataFrame validation, and the shared California reference geography. Shared code receives column names, patterns, paths, and thresholds as arguments; it never imports Population-and-Housing configuration.
- `scripts/pophousing/` holds E-5 and E-8 schemas, California geography rules, housing formulas, source-specific parsing, and pipeline-specific validation. It may import from `scripts/shared/` but nothing in `shared/` may import from it.
- `scripts/orchestrators/pophousing_pipeline.py` sequences the phases and owns error handling and the output summary. It contains no transformation logic of its own.

The dependency direction is strictly one-way: `shared helpers` are imported by `pophousing modules`, which are coordinated by the orchestrator. The same California geography reference in `scripts/shared/geography/california_geography.py` is deliberately shared with the Components of Change and Building Permits modules, so the county, region, and metro definitions have a single owner. This mirrors the structure adopted across the other module migrations; see [[projectSpec]] for the canonical five-phase pipeline model and the audit-status conventions.

---

## Technical Reference

The rest of this document walks every script the module comprises, grouped by the phase it serves, with configuration and shared helpers first because every phase depends on them. For each function it gives what the function does, the notable libraries it relies on, any performance or efficiency characteristic worth knowing, and what legacy code it was derived from. The legacy module was a small number of large files - `pophousing_pipeline.py` (the `main()` monolith), `data_cleaning_utils.py`, `enhanced_forward_fill_helpers.py`, `historical_data_processor.py`, `download_historical_data.py`, `run_original_pipeline.py`, `logging_config.py`, and `pophousing_config.py`. The refactor split those into the many small, single-responsibility modules below.

Every module follows the same file convention: a docstring header naming its data sources, outputs, usage, and test folder; banner comments separating sections; and one-line docstrings on each function that name the test file covering it.

## Configuration Modules

Configuration was one legacy file, `pophousing_config.py`, a flat list of module-level constants imported directly everywhere. The refactor keeps the raw constants in `lib/pophousing_config.py` but exposes them through four accessor modules that return defensive copies, so no pipeline module mutates shared state and each phase pulls only the slice of config it needs.

### `lib/pophousing_config.py`

The raw constant store. It defines the DoF base URL, request headers and timeout (pulled from the project-wide `lib/config.py`), all file paths, the fifteen raw E-5 column names, the region-to-county `REGIONS_MAPPING`, the county list, the town list, ambiguous city names, city-name mappings, historical name standardizations, and cache-age limits. This is the direct descendant of the legacy `pophousing_config.py`, trimmed to values that are genuinely Population-and-Housing-specific. It imports `get_default_http_settings` and `get_project_paths` from `lib/config.py` rather than hard-coding paths and headers.

> [!note] `HISTORICAL_DATA_PATH` is now a separate immutable baseline
> `HISTORICAL_DATA_PATH = HISTORICAL_BASELINE_PATH` (`PopHousing_Historical_E8.csv`), decoupled from `CURRENT_DATA_PATH`. The main pipeline reads it read-only; only the Phase 0 builder writes it. A sidecar (`HISTORICAL_BASELINE_METADATA_PATH`) records its coverage for a freshness check. This resolved the former self-perpetuating-history warning (A1).

### `config/paths.py`

One function, `get_paths()`, returns a dict of `pathlib.Path` objects for the project root, download directory, archive directory, current output path, historical input path, and deletion-log directory. It wraps the raw string constants from `lib/pophousing_config.py` in `Path`. Derived from the path constants that were scattered through the legacy config and `main()`.

### `config/sources.py`

`get_source_settings()` returns the DoF base URL, a fresh copy of the request headers, the timeout, the E-5 cache and fallback age limits, and the four regex patterns used to find the E-5 heading, landing page, workbook link, and filename. The cache/retention window is 90 days and the fallback window is 400 days; the fallback is deliberately wider than the cache so a workbook archived out of the active cache is still reachable during a DoF outage (B1). Derived from the scraping constants formerly embedded in the legacy download functions. Isolating them here is what lets the acquisition scraper take all its policy as arguments.

### `config/schemas.py`

`get_schema_config()` builds the largest config bundle: the raw E-5 column names, the `Region`/`City` to `County`/`Location` rename mapping, the "Alameda" data anchor, the numeric and zero-fill column lists, the summary and header row patterns, the meaningful-data columns, the full ordered `output_columns`, and three nested validation configs (`cleaning_validation`, `historical_validation`, and `final_validation`). `historical_validation` is the single acceptance gate for the E-8 baseline, shared by the main pipeline's Phase 1 and the Phase 0 builder; `final_validation` now also carries `valid_sources` (`E-5`, `E-8`, `Aggregated`) so the output's provenance column is checked. It imports `date` from `datetime` so the final validator's `maximum_year` is always the current year. Derived from the column lists and the many inline validation thresholds that lived in legacy `data_cleaning_utils.py` and `main()`. The single source of the output schema now lives here, which is what guarantees historical and modern frames share an identical shape before concatenation.

### `config/geography.py`

`get_geography_config()` composes the shared California reference (state name, county names, region names, region-to-county mapping) from `scripts/shared/geography/california_geography.py` with the Population-and-Housing-specific town list, ambiguous city names, protected "proper names ending in City," city-name mappings, and historical name standardizations, plus the valid levels, default level, and the San Joaquin population threshold used to disambiguate the city from the county. Derived from the geography constants in the legacy `pophousing_config.py` and the classification thresholds hard-coded in `enhanced_forward_fill_helpers.py`.

> [!note] Config is built once and threaded through the hot path
> `get_geography_config()` and `get_schema_config()` construct fresh dicts each call. The per-row classification loop no longer rebuilds the geography config: it is built once in `assign_missing_geographic_levels` and passed down to `classify_ambiguous_location`, and Phase 3/Phase 6 pass their existing config into `standardize_location_column` (A6). The remaining once-per-run leaf calls fetch their own config, which is negligible.

## Shared Helpers

These are the dataset-agnostic mechanisms in `scripts/shared/`. They were extracted from the generic operations buried inside legacy `data_cleaning_utils.py`, `download_historical_data.py`, and the inline file work in `main()`.

### `shared/downloads/http_downloads.py`

Imports `requests` and `pathlib`. Defines `HTTPDownloadError`, a single normalized exception for every request failure. `fetch_response(url, headers, timeout)` performs a GET, raises on HTTP errors, and translates `requests.Timeout`, `ConnectionError`, `HTTPError`, and the general `RequestException` into `HTTPDownloadError` with a descriptive message. `download_file(url, destination_path, headers, timeout)` streams the response to a `.part` temporary file and then atomically `replace`s it into place, cleaning up the temp file in a `finally` block so a failed download never leaves a half-written workbook. Derived from the raw `requests.get` calls duplicated across the legacy E-5 and E-8 downloaders. The atomic-write pattern is new and is the reason a mid-download crash cannot corrupt the cache.

### `shared/archives/file_retention.py`

Imports `re`, `shutil`, `time`, and `pathlib`. `find_files_older_than(directory, max_age_days, filename_pattern)` returns the sorted list of files whose name fully matches the pattern and whose modification time is at or beyond the age cutoff, validating the directory exists first. `archive_or_delete_files(file_paths, archive_directory)` moves each file into the archive with a numeric suffix if a name collides, or permanently deletes it when no archive directory is given, skipping files that no longer exist. Derived from the inline `os.rename` archive step and the deletion logic in legacy `cleanup_old_e5_files()`, generalized so any module can reuse it.

### `shared/data_cleaning/type_conversions.py`

Imports `pandas`. `parse_year_from_date(dataframe, date_col, out_col)` parses a date column with `pd.to_datetime(..., format="mixed")` and extracts a nullable `Int64` year. `coerce_numeric_columns(dataframe, numeric_cols)` strips thousands separators and coerces each named column with `pd.to_numeric(errors="coerce")`. Both return copies and validate that the columns exist. Derived from the repeated date-parsing and numeric-coercion snippets in legacy `data_cleaning_utils.py`.

### `shared/data_cleaning/row_filters.py`

Imports `pandas`. Four vectorized row filters, each returning a reindexed copy: `filter_year_range` keeps rows within inclusive year bounds; `remove_summary_rows` drops rows matching configured "Balance of" / "Incorporated" patterns while protecting values like "County Total" and "State Total"; `remove_header_like_rows` drops rows matching header patterns such as "Cities and Towns"; and `drop_empty_rows_without_data` drops rows that have neither a real location nor any positive numeric value across the configured data columns. Derived from the inline filtering in legacy `clean_common_e5()`, which did the same work row by row.

> [!note] Vectorized where the legacy code looped
> These filters use boolean-mask indexing rather than the legacy per-row Python loops, which is the largest single performance improvement in the cleaning path.

### `shared/data_cleaning/dataframe_operations.py`

Pure pandas, no imports beyond the DataFrame API. `forward_fill_columns(dataframe, columns)` forward-fills the named columns in a copy. `assign_values_from_mapping(dataframe, source_col, target_col, value_mapping)` maps source values into a target column while keeping unmatched originals. Derived from the forward-fill and conditional-assignment helpers in legacy `data_cleaning_utils.py`. `assign_values_from_mapping` is not used by this module but is consumed by the Components of Change census cleaner, which is exactly the cross-module reuse the shared tree is meant to enable.

### `shared/validation/dataframe_validators.py`

Imports `pandas`. Five generic checks: `validate_required_columns` returns missing columns; `validate_not_empty` reports whether any rows exist; `find_duplicate_rows` returns every row participating in a duplicate key; `validate_null_counts` returns per-column positive null counts; and `validate_numeric_range` returns rows outside optional inclusive bounds, optionally restricted to a row mask that must align with the frame's index. Derived from the seven inline checks in legacy `validate_historical_data()` and the range checks scattered through `main()`. These are the primitives the Population-and-Housing validators compose.

### `shared/geography/california_geography.py`

Imports the county list and region mapping from `lib/pophousing_config.py`. `get_california_geography()` returns the state name, the county set (with San Francisco added as a county), the region names, the region-to-county mapping, and the 26 CBSA metros with their metro-to-county and derived metro-to-region groupings. The helper `_derive_metro_to_region` verifies each metro nests within exactly one region and raises otherwise. Derived from the county and region constants formerly private to `pophousing_config.py`, plus the `msa_mapping` lifted out of the legacy Building Permits `permits_code.py`. This is the single shared owner of California geography for three modules.

### `shared/logging/pipeline_logging.py`, `shared/logging/dataframe_logging.py`, and `shared/logging/run_records.py`

These are now fully implemented (they were `pass`-body stubs when the migration plan was written). `pipeline_logging.py` provides `setup_logging`/`get_logger`/`close_logging` (a file + console logger with duplicate-handler guards) and `log_processing_step`; `dataframe_logging.py` provides `log_dataframe_info` and `log_data_quality_check`; all no-op safely when `logger is None`. `run_records.py` adds `execute_pipeline_run`, which wraps `main(logger=...)`, captures the outcome, and appends one JSONL record per run to `pipeline-runs.jsonl` (severity, phase, error detail, and fallback flags). The orchestrator's `__main__` entry runs through `execute_pipeline_run`, and `main` threads the logger through Phases 1, 3, and 6. This replaces the legacy `logging_config.py`. Structured-logging coverage is still uneven across phases — see "Flagged Issues and Fragilities" (A3).

## Phase 0: Building the Historical E-8 Baseline

This is the prerequisite that produces the historical CSV the six live phases consume. It replaces the legacy `download_historical_data.py` and `historical_data_processor.py`. It is a deliberately separate build (run manually, not on every pipeline run), now driven by `historical/build_baseline.py` and no longer an unwired collection of functions (A2). Because the E-8 workbooks come in three decade layouts, this side of the module carries the most format-detection logic.

### `historical/build_baseline.py`

The Phase 0 driver and the only writer of the immutable baseline. It runs in two modes: `--from-current` seeds the baseline from the current pipeline output's ≤2020 rows (the one-time migration onto the split-baseline layout), and the default mode rebuilds from the DoF E-8 decade workbooks, with `--download` to fetch them first. `map_historical_file_configs` turns `HISTORICAL_FILE_CONFIG` - which is keyed by filename and carries no `path`, `label`, or `recover_counties` - into the shape `build_historical_housing_dataset` expects, joining each filename to its download path, deriving an era label, and enabling county recovery on the 2010-2020 era. Every mode validates the result against the shared `historical_validation` gate before an atomic write and refreshes the sidecar. Derived from the previously unwired `build_historical_housing_dataset` + `download_historical_e8_files`, closing the A2 gap. See the runbook [[pophousing-standup]].

### `historical/baseline_metadata.py`

Reads, writes, and freshness-checks the baseline's JSON sidecar. `summarize_baseline_coverage` and `write_baseline_metadata` record the min/max year, row count, and build provenance; `check_baseline_freshness` compares the baseline's newest year against the newest `year_end` in `HISTORICAL_FILE_CONFIG` (a network-free signal) and returns an actionable warning when a new E-8 era has been configured but the baseline was never rebuilt. Phase 1 logs this check each run. New in this revision, supporting A1/A2.

### `acquisition/dof_historical_downloader.py`

Imports `re`, `pathlib`, `urllib.parse`, `bs4.BeautifulSoup`, and the shared HTTP helpers. Defines `E8DiscoveryError`. `get_historical_landing_page_urls(base_url, ...)` scrapes the DoF estimates page for the E-8 historical heading and returns the landing-page links beneath it. `find_geography_workbook_url(page_url, ...)` opens a landing page and returns the "Organized by Geography" workbook link, or `None` if absent. `download_historical_e8_files(download_dir, source_settings)` walks every landing page, downloads each geography workbook via the shared `download_file`, and continues past any single broken link so one dead report does not abort the batch. Derived from legacy `download_historical_data.py`, with the generic request and file-write mechanics moved into `shared/downloads/`.

### `historical/e8_format_detection.py`

Imports `pandas`. `detect_e8_file_format(raw_e8_df, search_rows=500)` scans up to 500 rows for the position of the "County Total" label: first column means the older 1990-2000 and 2000-2010 single-column layout (`old_format`), second column means the 2010-2020 layout that mirrors the modern E-5 (`new_format`), defaulting to `new_format`. Derived from the format-branching that was inline in legacy `historical_data_processor.py`. The label position is the cheapest reliable signal for distinguishing the layouts.

### `historical/e8_schema_normalizer.py`

Pure pandas. `normalize_e8_columns(raw_e8_df, format_config)` assigns a layout's positional column names to the first N columns, tolerating and dropping surplus trailing layout columns but raising if the frame is too narrow, then applies an optional rename mapping. Derived from the per-decade column-assignment code in legacy `historical_data_processor.py`. It is the E-8 analogue of the modern `e5_schema_normalizer.normalize_e5_columns`, but forgiving of extra columns because E-8 workbooks carry trailing junk.

### `historical/e8_era_cleaners.py`

Imports `pandas` and reuses the modern cleaning, classification, standardization, and metric functions plus the shared filters and type conversions - a deliberate demonstration that historical and modern data share one set of cleaners. It defines two column layouts (`_OLD_FORMAT_COLUMNS_12` and `_OLD_FORMAT_COLUMNS_13`, differing only in whether an explicit `Vacant Units` column is present).

- `clean_1990_2000` and `clean_2000_2010` both delegate to `_clean_old_format`, since those two decades share the single-column hierarchical layout.
- `clean_2010_2020` cleans the modern-style layout by reusing the exact E-5 steps: normalize columns, rename schema, forward-fill county, remove summary and header rows, forward-fill locations, drop empty rows, build county context, coerce numerics, derive housing columns, then classify and standardize. It keeps the raw date in the `Year` column so that census (April 1) rows can be dropped later during standardization.
- `_clean_old_format` flattens the single-column layout: it picks the 12- or 13-column schema by width, blanks placeholder locations, coerces numerics, derives `Vacant Units` when absent, identifies county-header rows, attaches each block's parent county, drops the header rows, forward-fills locations, drops empty rows, and classifies and standardizes.
- `_attach_block_county` forward-fills the parent county down each block, resetting to NA at state-total boundaries.
- `_classify_and_standardize` is the shared tail both paths call: resolve county-total rows, normalize state-total rows, assign missing levels, apply town overrides, sanitize levels, standardize names, and remove balance rows.

Derived from the three decade-specific branches of legacy `historical_data_processor.py`, refactored so the decade-specific work is only the column layout and the block-county handling, and everything downstream is the shared classification tail.

### `historical/e8_standardization.py`

Imports `pandas` and the shared `coerce_numeric_columns`. `extract_annual_year(date_series)` parses dates and returns the year, masking April 1 census rows and unparseable dates to NA so the January 1 annual estimate is the canonical row for each year. `standardize_e8_data(df, year_start, year_end)` parses years, drops census rows, bounds the frame to the era's years, coerces numerics, and scales pre-2020 fractional vacancy rates to percentages via `_normalize_decimal_vacancy_rates`. Derived from the common post-clean standardization step in legacy `historical_data_processor.py`. The census-row drop is the historical analogue of a subtlety that the modern E-5 does not have, since E-5 carries only annual estimates.

### `historical/boundary_year_resolution.py`

Pure pandas. `resolve_boundary_year_overlaps(df, source_priority)` keeps one row per `(Location, Level, Year)` when a boundary year such as 2000 or 2010 appears in two adjacent decade workbooks, preferring the higher-priority (more recent) source. It does this with a stable-sort-then-`drop_duplicates` strategy, defensively generating non-colliding temporary priority and original-order column names, and validates that every observed source is named in the priority list. Derived from the boundary-overlap resolution in legacy `historical_data_processor.py`. The same stable-sort dedup pattern is reused verbatim by the modern `resolve_source_overlap` and `deduplicate_geographic_rows`.

### `historical/missing_county_recovery.py`

Imports `pandas` and reuses the E-8 normalizer, the E-5 schema rename, the housing-metric derivation, the annual-year extractor, and the shared forward-fill and coercion helpers. `extract_missing_county_rows(raw_e8_df, target_years)` pulls the "County Total" rows for target years straight from the raw 2010-2020 workbook, promoting each to a real County row - recovering county totals that the era cleaner drops. `integrate_missing_county_rows(historical_df, missing_county_df)` appends only those recovered rows whose keys are not already present, computing existing keys as a set of tuples for an O(1) membership test. Derived from the missing-county repair step in legacy `historical_data_processor.py`.

### `historical/historical_pipeline.py`

Imports `pandas`, the schema config, and every historical module above. `build_historical_housing_dataset(file_configs)` is the Phase 0 entry point: it normalizes the per-era configs, loads each workbook (or accepts a pre-read frame for testing), runs the era cleaner, standardizes, tags each era with its source label, concatenates, resolves boundary overlaps, recovers missing counties, resolves overlaps again, and finalizes to the canonical schema and sort order. The private helpers `_normalize_file_configs`, `_resolve_clean_func`, `_load_raw_workbook`, and `_finalize_historical_dataset` handle config shapes, cleaner lookup by name or callable, workbook reading, and final schema enforcement. Derived from the top-level driver logic of legacy `historical_data_processor.py`. It is now invoked by `historical/build_baseline.py` (the Phase 0 driver), which maps `HISTORICAL_FILE_CONFIG` into the shape this function expects; the earlier "no non-test caller" gap is resolved (A2).

## Phase 1: Setup and Validation

### `archives/e5_retention.py`

Imports `math`, `re`, `time`, `datetime`, `pathlib`, and the shared `file_retention` helpers. `cleanup_old_e5_files(...)` lists cached E-5 workbooks, optionally writes deletion warnings, then archives the expired ones via `find_files_older_than` and `archive_or_delete_files`. `write_deletion_warnings(file_paths, warning_days, deletion_log_directory, max_age_days=60)` computes each file's remaining days with `math.ceil` and writes one idempotent warning file per configured threshold, sanitizing the filename and skipping a warning that already exists. Derived from legacy `cleanup_old_e5_files()`, which did all of this inline; the generic age-lookup and disposition mechanics now live in `shared/archives/`.

### `validation/historical_data_validator.py`

Imports `pandas`, `pathlib`, and the shared validators. `validate_historical_housing_data(file_path, validation_config)` loads the historical CSV and runs the full battery: not-empty, required columns, valid and complete years, expected geographic levels, no negative populations, a minimum count of California state records, a plausible recent-state-population mean, per-column null limits, and duplicate keys. It returns `(is_valid, messages)` rather than raising, so the orchestrator decides how to react. Derived from the legacy `validate_historical_data()` worker, with the seven generic checks now delegated to `shared/validation/dataframe_validators.py` and only the Population-and-Housing-specific composition kept here.

## Phase 2: Data Acquisition

### `acquisition/dof_e5_downloader.py`

Imports `re`, `time`, `pathlib`, `urllib.parse`, `zipfile.BadZipFile`, `pandas`, `bs4.BeautifulSoup`, and the shared HTTP helpers. Defines `E5DiscoveryError` and the module-level `E5_FILENAME_PATTERN`.

- `get_e5_file_url(source_settings)` scrapes the estimates page for the E-5 heading, then either returns a direct workbook link found in the heading's list, or follows a year-range landing page and finds the workbook there. It raises `E5DiscoveryError` at each point discovery can fail. Derived from the legacy `get_e5_file_url()` two-request scraper.
- `get_e5_filename_from_url(url, filename_pattern=...)` extracts the filename from a URL path, URL-decoding it, and validates it against the E-5 pattern. Pure string work, derived from the legacy three-line worker of the same name.
- `download_e5_data(url, download_directory, cache_max_age_days, headers=None, timeout=60)` returns cached data when a fresh, readable workbook already exists, otherwise downloads and reads it. It tolerates a corrupt cache file by catching `BadZipFile`/`OSError`/`ValueError` and falling through to a fresh download. Derived from the legacy `download_e5_data()`, which handled caching and reading inline.
- `get_most_recent_e5_file(download_directory, filename_pattern, fallback_max_age_days)` is the fallback path: it sorts matching cached workbooks newest-first and returns the first one within the age limit that reads cleanly. Derived from the legacy `get_most_recent_e5_file()`.
- `_read_e5_workbook(workbook_path)` opens the `.xlsx` with `pd.ExcelFile`, requires at least two sheets, and reads the second sheet, raising a clear error if `openpyxl` is missing. This centralizes the "data lives on sheet two" rule.

The generic request and binary-write mechanics are gone from this file entirely; it composes `fetch_response` and `download_file` from `shared/downloads/`.

## Phase 3: E-5 Cleaning

This phase is coordinated by `e5_pipeline.py` and draws on five cleaning and calculation modules plus a validator. Together they replace legacy `clean_e5_data()`, `clean_common_e5()`, `add_geographic_level_e5()`, the classification helpers in `enhanced_forward_fill_helpers.py`, and the cleaning parts of `data_cleaning_utils.py`.

### `cleaning/e5_schema_normalizer.py`

Pure pandas. `normalize_e5_columns(raw_e5_df, column_names)` assigns the fifteen configured names after asserting the worksheet width matches exactly. `trim_to_first_data_row(raw_e5_df, anchor_value, column)` drops the metadata rows above the first "Alameda" row. `rename_e5_schema(raw_e5_df, mapping)` renames `Region`/`City` to `County`/`Location`. Derived from the column-assignment, header-trimming, and rename work that opened legacy `clean_e5_data()`.

### `cleaning/hierarchical_location_cleaning.py`

Imports `pandas` and `get_geography_config`. `identify_county_headers(df, county_names, location_col)` finds county-name rows that are followed within ten rows by a "County Total," marking them as headers; it is a Python scan with a bounded lookahead. `forward_fill_locations_with_context(df, location_col, county_col)` blanks empty location labels and forward-fills them - a vectorized `ffill` that replaces the legacy ~100-line row-by-row loop. `build_county_context_column(df, location_col, county_col, temp_col)` creates the `_temp_county` column used later to turn "County Total" rows into named counties, using a vectorized fill when a county column exists and a header-scan loop otherwise. Derived from `enhanced_forward_fill_helpers.py` and the forward-fill loop inside legacy `clean_common_e5()`. (The former `has_meaningful_housing_data` row-level check was removed as dead parity code - A4 - since the vectorized `drop_empty_rows_without_data` fully subsumes it.)

### `cleaning/geographic_classification.py`

Imports `pandas` and `get_geography_config`. This module owns all geographic-level logic.

- `classify_ambiguous_location(location, county_context, population, config)` resolves a name that could be a city or a county using the explicit `County Total`/`State Total` labels, the town list, and the San Joaquin population threshold. It now receives the geography config as an argument rather than rebuilding it (A6). Its former ten-row "County Total" lookahead was removed as redundant (A5): by the time it runs, `resolve_county_total_rows`/`normalize_state_total_rows` have already promoted the real County/State rows. To rebuild the lookahead if a future input needs it, pass the frame and the current row index in, locate the row with `df.index.get_loc`, and return `"County"` when any of the next ten `Location` values equals `"County Total"`.
- `assign_geographic_level_with_context(location, county_context, population, housing_row, geography_config)` is the row classifier used across the module: State, Region, County, Town, ambiguous, or the default City. It receives config as an argument.
- `resolve_county_total_rows` replaces "County Total" labels with the block's county name and sets the level to County.
- `normalize_state_total_rows` renames "State Total" to California and sets the level to State.
- `assign_missing_geographic_levels(df, classifier_fn, ..., geography_config=None)` classifies only the rows whose level is still missing. It vectorizes the unambiguous cascade (State/Region/County/Town/default) with boolean masks over the whole missing subset and reserves the per-row `classifier_fn` for the small minority of genuinely ambiguous names (B6). The config is built once and threaded through (A6). A parity test asserts the vectorized result equals a straight per-row application of `assign_geographic_level_with_context`.
- `apply_town_overrides` forces configured towns to the Town level.
- `sanitize_geographic_levels` replaces any invalid level with the default.
- `remove_balance_rows` drops "Balance of" summary rows.
- `drop_helper_columns` removes the temporary `County` and `_temp_county` columns.
- `standardize_san_francisco_classification` duplicates San Francisco into a City row and a County row, deduplicating first so it never expands more than once.

Derived from legacy `add_geographic_level_e5()`, `add_geographic_level()`, `standardize_san_francisco_classification()`, and the classifier functions in `enhanced_forward_fill_helpers.py`. Keeping one classifier for both historical and modern data is the reason the historical era cleaners can import this module directly.

> [!note] The ambiguous-location lookahead was removed
> `classify_ambiguous_location` no longer takes a frame or does a "County Total" neighbor scan (A5). The county/state promotion upstream already covers those cases; the rebuild recipe is documented in the function's description above and in the code comment.

### `cleaning/location_standardization.py`

Imports `re`, `pandas`, and `get_geography_config`. `standardize_location_column(df, location_col, geo_col, only_levels)` maps city and town names through the canonical and historical name mappings and strips a trailing " City" suffix unless the name is a protected proper name (like "Daly City"), applying only to the selected levels via an inner `standardize_name` helper. Derived from the inline `clean_name()` inside legacy `clean_common_e5()` and the separate legacy `standardize_city_names()`, consolidated into one implementation that both Phase 3 and Phase 6 call.

### `calculations/housing_metrics.py`

Imports `numpy` and `pandas`. `add_housing_derived_columns(df)` computes `Single Family Units`, `Multiple Family Units`, and `Vacant Units` from the raw sub-columns, coercing to numeric and filling NA with 0. `recalculate_housing_rates(df, row_mask)` recomputes `Vacancy Rate (%)` and `Persons Per Household` for the masked rows using `np.where` to guard against division by zero, and requires the mask to align with the frame's index. Derived from the derived-column and rate calculations that were duplicated across legacy cleaning, regional, and state code; this module is now their single implementation. `add_housing_derived_columns` runs in Phase 3; `recalculate_housing_rates` is called from the Phase 5 aggregators.

### `validation/cleaning_validators.py`

Imports `pandas`. `validate_cleaned_e5_data(df, validation_config)` checks the cleaned frame for emptiness, required columns, null critical columns, duplicate keys, invalid levels, and negative numerics, returning `(is_valid, messages)`. Derived from the validation tail that legacy `clean_e5_data()` performed inline.

### `cleaning/e5_pipeline.py`

The Phase 3 orchestrator. `clean_e5_data(raw_e5_df, schema_config, geography_config)` runs the full sequence described in the phase narrative - normalize, trim, rename, forward-fill county, filter summary and header rows, forward-fill locations, drop empties, build county context, parse and filter year, coerce numerics, zero-fill, derive housing columns, resolve county and state totals, assign missing levels, apply town overrides, sanitize levels, standardize names, remove balance rows, drop helpers, set `Source` to `E-5`, enforce output columns, and validate - raising if validation fails. It imports from every Phase 3 module plus the shared filters, type conversions, and operations. Derived from the legacy `clean_e5_data()` hybrid function, which mixed orchestration with inline transformation; the transformation now lives in the imported modules and this file is pure sequencing.

## Phase 4: Merge Historical and Modern

### `merging/historical_modern_merge.py`

Imports `pathlib`, `pandas`, and the schema config. `load_historical_housing_data(historical_file_path)` reads the historical CSV, checks it is non-empty and schema-complete, selects the canonical columns, and labels every row `E-8`. `filter_historical_years(df, max_year)` validates the `Year` column, converts it to `Int64`, and keeps rows at or before the boundary. `merge_historical_and_modern_data(historical_df, modern_df)` verifies both frames have identical column sets, aligns column order, and concatenates. `resolve_source_overlap(df, key_columns, source_priority)` deduplicates overlapping keys by the `E-5`-over-`E-8` priority, using the same stable-sort strategy and defensive temp-column naming as the historical boundary resolver, and validates there are no null keys or unknown sources. Derived from the inline historical load, filter, concat, and `drop_duplicates` that legacy `main()` performed in Phase 4.

## Phase 5: Enrichment

### `aggregation/aggregation_utils.py`

Imports `pandas`. `remove_existing_geographic_level(df, level_col, level_name)` drops rows of a given level so aggregates can be rebuilt cleanly. `deduplicate_geographic_rows(df, location_col, year_col, level_col, preferred_level)` keeps one row per location-year, preferring a chosen level, via the stable-sort dedup pattern. `detect_additive_columns(df, group_col, excluded_columns)` returns the columns that are safely additive (every originally non-null value converts to a number); `_aggregate_additive_columns(df, group_col, excluded_columns, additive_columns=None)` sums those columns by group with `groupby(...).sum(min_count=1)`. Splitting detection from summation lets `build_regional_rows` detect the additive set once on the deduplicated county frame and reuse it across all nine regions instead of re-detecting per region (B7). Derived from the deduplication and groupby-aggregation logic inside legacy `add_regional_data()` and `add_state_data_for_modern_years()`.

### `aggregation/regional_aggregation.py`

Imports `pandas`, the aggregation helpers, and `recalculate_housing_rates`. `build_regional_rows(df, regions_mapping, ...)` deduplicates county rows, sums each region's member counties into a Region row, blanks the rate columns (to be recomputed), and labels the source `Aggregated`. `add_regional_data(df, regions_mapping)` strips existing Region rows, rebuilds them, concatenates, and recomputes the two rates for the new Region rows. Derived from legacy `add_regional_data()`, with the shared additive-sum and dedup moved into `aggregation_utils` and the rate recomputation moved into `housing_metrics`.

### `aggregation/state_aggregation.py`

Imports `pandas`, the aggregation helpers, and `recalculate_housing_rates`. `find_missing_state_years(df, state_name, year_col)` returns county years that lack a matching California state row. `build_state_rows_from_counties(df, missing_years, state_name)` sums all counties for those years into State rows with blanked rates and an `Aggregated` source. `add_state_data_for_missing_years(df, state_name)` appends those rows and recomputes their rates, masking exactly the newly added rows by index position. Derived from legacy `add_state_data_for_modern_years()`.

### `calculations/rate_normalization.py`

Imports `pandas`. `find_decimal_fraction_rates(df, year_col, rate_col, level_col, min_year)` decides per vintage rather than per row (B5): for each recent year it flags rates only when that year's median rate is below 1.0 (the whole vintage is fraction-encoded), and within such a vintage scales only rows still below 1.0. This means a genuine sub-1% vacancy inside an otherwise percent-encoded year is left alone instead of being rescaled to tens of percent. `normalize_decimal_fraction_rates(df, rate_col, mask)` multiplies the masked rates by 100 and rounds, requiring the mask to align with the index. Derived from legacy `fix_vacancy_rate_decimal_fractions()`, split into a pure detector and a pure transformer.

### `validation/aggregation_validators.py`

Imports `pandas` and the shared `validate_numeric_range`. `validate_normalized_housing_rates(df, year_col, rate_col, level_col)` confirms every vacancy rate is within 0 to 100 and that no recent non-state rate still looks like a decimal fraction, returning `(is_valid, messages)`. Derived from the validation portion of legacy `fix_vacancy_rate_decimal_fractions()`, composed on top of the shared range validator.

## Phase 6: Finalize and Output

Phase 6 reuses the Phase 3 classification and standardization functions as a final normalization pass, then finalizes and writes. The reused functions - `assign_missing_geographic_levels`, `standardize_location_column`, and `standardize_san_francisco_classification` - are documented under Phase 3; the point of the refactor is that Phase 6 owns no duplicate copies of them.

### `output/finalize_dataset.py`

Imports `pathlib` and `pandas`. `prepare_housing_output(df, output_columns, sort_columns)` validates and stringifies the year, enforces the exact output column order, and sorts, all without mutating the input. It no longer overwrites `Source`: the per-row `E-5` / `E-8` / `Aggregated` provenance from earlier phases flows through to the output (B3), and `Source` is now a required output column. `write_housing_output(df, output_path)` writes to a `.tmp` file and atomically `replace`s it into the final path, cleaning up on failure. Derived from the inline source-setting, column-reordering, sorting, and CSV-writing at the end of legacy `main()`. The atomic write is new and is what makes the "archive then overwrite" step safe.

### `validation/final_dataset_validator.py`

Imports `pandas` and the shared validators. `validate_final_housing_dataset(df, validation_config)` is the most thorough check in the module: schema completeness, duplicate output keys, null and invalid and missing geographic levels, null or unknown `Source` values (the new `valid_sources` gate - B3), presence of a California state row, a complete year span with no future years, the San Francisco City-and-County invariant (and never more than twice per year), a Bay Area 2020 population sanity range, and nonnegative and in-range numeric columns. It returns `(is_valid, messages)`. Derived from the final duplicate check, Bay Area sanity check, and schema enforcement that legacy `main()` did inline before writing.

### `orchestrators/pophousing_pipeline.py`

Imports `pandas` and every phase entry point. `main(logger=None)` runs the six phases inside per-phase `try/except` blocks, wrapping any failure in `PipelinePhaseError` via `_raise_phase_error` so a failure is always attributed to a named phase. It owns the cold-start baseline check and freshness log, the E-5 acquisition fallback chain (discover, download, most-recent-cache-or-archive), the merge keys and source priority, the enrichment calls, the Phase 6 missing-level invariant assertion, the final normalization and validation, the archive-then-write, and the returned summary dict. The acquisition guard now also catches read/parse failures on a fresh download and records `e5_discovery_failed`/`e5_download_failed` flags in the summary so a degraded run logs as `recovered` (B2). It emits `log_processing_step` at every phase boundary (Phases 1-6) and contains no transformation logic. Derived from the legacy `main()` monolith and the subprocess-based `run_original_pipeline.py`, both of which are fully replaced; the interactive "continue after failure?" prompt of the legacy runner is gone in favor of structured exceptions.

## Flagged Issues and Fragilities

These were noted while documenting the module and then re-examined in a dedicated reliability, robustness, and efficiency audit (2026-07-10). Each entry states the problem and a detailed resolution plan, and calls out any decision that was the maintainer's to make. The section is in two parts — **Part A** revisits the issues flagged during the original documentation pass, and **Part B** records findings from the audit.

> [!success] All backend items implemented (2026-07-11)
> Every issue below (A1, A2, A4, A5, A6, B1-B8) has been implemented in code, with tests, per the maintainer's recorded decisions. The problem statements and plans are kept for the record; the Open Items checklist at the end is fully checked. The two frontend deliverables - the "update data" button (A2) and the `Source` multi-select (B3) - are the only follow-ups, tracked separately. Standing the pipeline up from scratch is documented in the runbook [[pophousing-standup]].

> [!info] Status note on the technical-reference body
> The "Shared Helpers → `shared/logging/`" subsection above has been corrected to describe the logging modules as implemented (they were `pass`-body stubs in the original migration plan). See A3 for the current state and the small residual-coverage cleanup.

### Part A — Previously Flagged Issues

#### A1. The historical baseline is the pipeline's own prior output (self-perpetuating history + cold-start gap)

**Problem.** `HISTORICAL_DATA_PATH = CURRENT_DATA_PATH` in [lib/pophousing_config.py](../../../../lib/pophousing_config.py) (line 28), so Phase 4 loads `PopHousing_Current.csv`, filters it to years ≤ 2020, and Phase 6 writes back to the same file. The pre-2020 history therefore comes from the previous run's output, not from an immutable canonical E-8 file. Two concrete risks follow:

1. **Silent corruption drift.** If a bad run ever writes malformed pre-2020 rows and passes the Phase 1 validator, that corruption becomes the next run's "history." The Phase 1 historical validator (`validate_historical_housing_data`) is the *only* guard, and its checks are coarse (year coverage, level presence, a state-population mean band, zero nulls in required columns). A subtle numeric corruption inside plausible bounds would survive.
2. **Cold-start failure.** On a machine where `PopHousing_Current.csv` does not yet exist, Phase 1 calls `validate_historical_housing_data(paths["historical_data_path"], ...)`, which raises `FileNotFoundError` and aborts the whole run before any scraping. The pipeline cannot bootstrap itself; it depends on the Phase 0 seed having been run and committed first (see A2). This is the same pattern noted for Building Permits.

**Resolution plan.**
- Give deep history its own immutable path. Add a distinct `HISTORICAL_BASELINE_PATH` constant (e.g. `.../housing-population/PopHousing_Historical_E8.csv`) and point `config/paths.py`'s `historical_data_path` at it, decoupling it from `current_data_path`. The Phase 0 builder writes *only* this file; the main pipeline reads it and never writes it.
- Because the baseline is now write-once, the pre-2020 slice is no longer re-derived from the previous output each run, which removes the drift channel entirely and makes the Phase 1 validator a true acceptance gate on an external artifact rather than a self-check.
- Handle cold start explicitly: if the baseline file is absent, emit a clear, actionable error ("historical E-8 baseline not found; run the Phase 0 builder — see A2") rather than a bare `FileNotFoundError`.
- Migration step: copy the current `PopHousing_Current.csv`'s ≤2020 rows into the new baseline path once, verify it passes `validate_historical_housing_data`, then commit it as the seed.

> [!question] Decision for you (A1)
> Do you want the historical baseline stored as a **committed artifact in the repo/data tree** (reproducible, versioned, but must be refreshed by hand) or **regenerated on demand** by wiring Phase 0 into a scheduled/manual job (A2)? The two are complementary; the immediate fix is the separate path, and A2 decides how it gets refreshed.

> [!note] Response to A1
> Store the historical E-8 baseline as a committed, versioned artifact, while providing a manual Phase 0 command that can regenerate it from the source workbooks. The committed baseline should be the normal pipeline’s read-only historical input so fresh installations remain reproducible and do not depend on DoF availability. Phase 0 should be run only when the historical source changes or the baseline needs to be intentionally rebuilt; its output should be validated, reviewed as a data diff, and then committed. Add a freshness warning so an outdated baseline is visible without making every pipeline run rebuild historical data.

#### A2. The Phase 0 historical build is not wired into any orchestrator

**Problem.** `build_historical_housing_dataset` (in `historical/historical_pipeline.py`) and `download_historical_e8_files` (in `acquisition/dof_historical_downloader.py`) have no non-test callers. Refreshing the baseline is a manual operation with no automated freshness check against the latest E-8 release. Worse, the config that *should* drive it — `HISTORICAL_FILE_CONFIG` in [lib/pophousing_config.py](../../../../lib/pophousing_config.py) — is not in a shape the builder accepts: `_normalize_file_configs` derives `label` from each dict key (the filename), but the per-era dicts carry no `path` and no `dataframe`, so `_load_raw_workbook` would raise `ValueError("…has neither 'dataframe' nor 'path'")`. There is also no `recover_counties` flag on the 2010-2020 entry, so `extract_missing_county_rows` would never run in a config-driven build. In other words, the wiring is not just absent — the existing config cannot be handed to the builder as-is.

**Resolution plan.**
- Write a small Phase 0 driver script (e.g. `scripts/pophousing/historical/build_baseline.py`) that: (1) calls `download_historical_e8_files(download_dir, source_settings)` to fetch the three workbooks, (2) maps `HISTORICAL_FILE_CONFIG` into the builder's expected shape — join each filename key to its full download path, carry `sheet_name`/`header`/`year_start`/`year_end`, add `recover_counties: True` on the 2010-2020 entry, and let `label` default to the era, (3) calls `build_historical_housing_dataset(file_configs)`, (4) validates the result with `validate_historical_housing_data` before writing, and (5) writes atomically to the new `HISTORICAL_BASELINE_PATH` from A1.
- Add a freshness check: record the E-8 release identifier (or the newest `year_end`) alongside the baseline (a sidecar JSON or a row-count/max-year assertion), and have the main pipeline log a warning when the baseline looks older than the newest E-8 landing page discovered by `get_historical_landing_page_urls`.
- Document the operation in the module README and in this guide's "How the Pipeline Runs" so a reader knows deep history is a separate, deliberate build.

> [!question] Decision for you (A2)
> Should the Phase 0 build be **fully manual** (a documented command run when DoF publishes a new E-8 decade, ~once every 10 years plus corrections) or **scheduled** (e.g. an annual CI job that rebuilds and diffs)? Given E-8 changes rarely, a documented manual command plus the freshness warning is the lower-cost option and my recommendation; a scheduled job is only worth it if you want zero-touch guarantees.

> [!note] Response to A2
> The Phase 0 build should be manual with a freshness warning. The front end needs to have an update data button so that a researcher can easily update it without needing to open the codebase or run any code themselves.

#### A3. Structured logging — RESOLVED since the original flag

**Update.** This is no longer an open issue. `shared/logging/pipeline_logging.py`, `dataframe_logging.py`, and a new `shared/logging/run_records.py` are fully implemented: `setup_logging`/`get_logger`/`close_logging` configure a file+console logger with duplicate-handler guards; `log_processing_step`, `log_dataframe_info`, and `log_data_quality_check` emit structured records (all no-op safely when `logger is None`); and `execute_pipeline_run` wraps `main`, appends one JSONL record per run to `pipeline-runs.jsonl` (with severity, phase, error detail, and fallback flags), and is the actual `__main__` entry point of the orchestrator. `main(logger=None)` threads the logger through Phases 1, 3, and 6.

**Residual cleanup (small).**
- Logging coverage is uneven: Phases 2, 4, and 5 emit no `log_processing_step`/`log_dataframe_info` calls, so the structured log has gaps around acquisition, merge, and enrichment. Add step logs at those boundaries for parity.
- The E-5 acquisition fallback chain in Phase 2 (discovery failed → download failed → most-recent-cache) sets **no** summary flag matching `run_records`' `FALLBACK_FLAG_PATTERN` (`_failed`/`_used_manual`/`source_failed`), so a run that silently fell back to a stale cached workbook is recorded as a clean `success`, not `recovered`. Have Phase 2 record a flag such as `e5_discovery_failed` / `e5_used_fallback_cache` in the returned summary so the run-record severity reflects reality. (This overlaps B2.)
- The stale "unimplemented stubs" language in the technical-reference body has been corrected (this revision); the only logging work left is the per-phase coverage gaps above.

> [!note] Response to A3
> Looks good

#### A4. `has_meaningful_housing_data` is retained but unused in the live path

**Problem.** The function exists in `hierarchical_location_cleaning.py` and is tested, but the wired pipeline uses the vectorized `drop_empty_rows_without_data` instead. It is parity code for a row-level caller that does not exist.

**Resolution plan.** Low stakes; pick one:
- **Remove it** (and its test) to reduce surface area, since `drop_empty_rows_without_data` fully subsumes it. Recommended unless a row-level caller is planned.
- **Or keep it** and add a one-line docstring note that it is a row-level convenience not used by the vectorized pipeline, so future readers don't wire it back in by mistake.

No decision is load-bearing here; either is fine. Flagging only so it isn't mistaken for dead-but-necessary code.

> [!note] Response to A4
> Remove it

#### A5. The ambiguous-location lookahead is bypassed during the missing-level pass

**Problem.** `assign_geographic_level_with_context` calls `classify_ambiguous_location(..., None, None)`, hard-passing `housing_df=None` and `row_index=None`, which disables that function's ten-row "County Total" neighbor scan. In the `assign_missing_geographic_levels` loop — the only live caller — ambiguous names (e.g. `Alameda`, `Orange`, `San Joaquin`) are therefore resolved by the town list and the San Joaquin population threshold *only*, never by the neighbor scan. The lookahead branch is reachable solely when `classify_ambiguous_location` is called directly with a DataFrame, which the live pipeline never does.

**Why it usually doesn't bite.** By the time `assign_missing_geographic_levels` runs in Phase 3, `resolve_county_total_rows` and `normalize_state_total_rows` have already promoted the County/State rows using `_temp_county` context, so the county reading of an ambiguous name is generally already set and the row is no longer "missing." The bypass matters only for an ambiguous city/county name that reached the classifier still unlabeled and is *not* San Joaquin — a narrow case.

**Resolution plan.**
- **Confirm intent first.** If the county-total promotion already covers every real ambiguous-county case (very likely, given the E-5 hierarchy), then the lookahead is genuinely redundant in this path and the cleanest fix is to *delete the dead parameters* (`housing_df`, `row_index`) from `classify_ambiguous_location` and the `None, None` call site, so the function's contract stops advertising a capability it never exercises.
- **If you want the lookahead active:** thread the frame and index through. Change `assign_geographic_level_with_context`'s signature to accept the DataFrame and the current row index, and have `assign_missing_geographic_levels` pass `result` and `row_index` when it invokes the classifier. Note the cost: the neighbor scan is O(rows) per ambiguous row, turning the classification loop into O(n²) in the worst case — acceptable only because ambiguous rows are few. This interacts with B6 (vectorizing the loop), so decide A5 and B6 together.

> [!question] Decision for you (A5)
> Is the ten-row "County Total" lookahead **intended to be live** during the missing-level pass, or is it legacy behavior that the county-total promotion has made redundant? If redundant → delete the dead path. If intended → thread the frame through and accept the per-row scan cost.

> [!note] Response to A5
> Remove the lookahead but include its functions in enough detail for it to be rebuilt by another programmer quickly if necessary in this guide.

#### A6. Config bundles are rebuilt on every call

**Problem.** `get_geography_config()` is invoked *inside* `classify_ambiguous_location`, `assign_missing_geographic_levels`, `build_county_context_column`, and `standardize_location_column` rather than being threaded through as an argument. During the per-row classification loop, `classify_ambiguous_location` rebuilds the whole geography dict (sets, mappings, and a `_derive_metro_to_region` validation pass over 26 metros) on every ambiguous row. `get_california_geography()` re-runs `_derive_metro_to_region` — which raises if any metro fails to nest — on each call, so this is not free.

**Resolution plan.**
- Thread config down instead of rebuilding: `assign_missing_geographic_levels` already builds `geography_config` once and passes it to `assign_geographic_level_with_context`; extend that so `classify_ambiguous_location` receives the same dict rather than calling `get_geography_config()` itself. Do the same for `build_county_context_column` (accept `county_names` as an argument, as the schema-driven callers already have the config in hand) and `standardize_location_column` (accept the three mappings/sets it needs).
- Alternatively, memoize the two builders with `functools.lru_cache` (returning immutable/frozen structures so callers can't mutate the shared copy). This is a smaller change but weakens the "defensive copies" guarantee the config layer was built for — prefer threading for the hot loop and reserve caching for the leaf builders if profiling still shows cost.
- This is a real but modest cost; fix it opportunistically alongside B6, since both touch the classification loop.

> [!note] Response to A6
> Thread config down instead of rebuilding

### Part B — Additional Audit Findings

#### B1. E-5 retention age equals fallback age, so the fallback can never reach past the cache window (robustness)

**Problem.** `E5_CACHE_MAX_AGE_DAYS = 60`, `E5_FALLBACK_MAX_AGE_DAYS = 60`, and Phase 1 calls `cleanup_old_e5_files(..., max_age_days=60)` — all three thresholds are equal. Phase 1 runs *before* acquisition and archives every E-5 workbook older than 60 days out of the download directory. Then in Phase 2, if discovery and download both fail, `get_most_recent_e5_file` looks for a cached workbook **≤ 60 days old** — but any file older than 60 days has already been moved to the archive and is therefore invisible to the fallback. Because DoF publishes E-5 annually (≈ May), the realistic failure scenario is: last successful download was 90+ days ago, DoF's page is briefly down, and the fallback finds nothing — even though a perfectly usable 90-day-old workbook exists in the archive. The fallback's entire purpose (survive a transient DoF outage) is defeated for exactly the part of the year it is most needed. A secondary drift: the `E5_CACHE_MAX_AGE_DAYS` comment says "90 days" while the value is 60.

**Resolution plan.**
- Make the fallback window strictly larger than the retention window so a retained-then-archived file is still reachable as a fallback. Concretely: raise `E5_FALLBACK_MAX_AGE_DAYS` to cover the annual gap (e.g. 400 days), or — cleaner — have `get_most_recent_e5_file` also search the **archive directory**, not just the download directory, so retention and fallback stop fighting.
- Alternatively, reorder so acquisition runs before retention cleanup, guaranteeing the current workbook is never archived out from under the fallback in the same run. (Weaker; the cross-run annual gap remains.)
- Fix the stale "90 days" comment to match whatever value is chosen, and add a test asserting `fallback_max_age_days > cleanup max_age_days`.

> [!question] Decision for you (B1)
> Preferred fix: **widen the fallback window** (simple constant change) or **let the fallback search the archive** (keeps retention aggressive but makes archived workbooks recoverable)? I recommend searching the archive — it keeps the 60-day cache discipline while giving the fallback real reach.

> [!note] Response to B1
> Hybrid aproach: Set E5_CACHE_MAX_AGE_DAYS = 90 and E5_FALLBACK_MAX_AGE_DAYS = 400. Keep the active cache clean, but allow the fallback function to search both locations.

#### B2. A freshly-downloaded but corrupt E-5 aborts the pipeline instead of falling back (robustness)

**Problem.** In Phase 2 the orchestrator wraps `download_e5_data(...)` in `try/except HTTPDownloadError` only. But after a successful HTTP download, `download_e5_data` calls `_read_e5_workbook`, which can raise `ValueError` (workbook has < 2 sheets, or a truncated/corrupt `.xlsx`) or `RuntimeError` (openpyxl missing). Those are **not** `HTTPDownloadError`, so they escape the inner guard, skip the `get_most_recent_e5_file` fallback entirely, and propagate to the outer `except Exception` → `Phase 2 failed`. A single malformed fresh download therefore aborts the run even when a good cached workbook is sitting right there. (`download_e5_data` does tolerate a corrupt *cached* file by catching `BadZipFile/OSError/ValueError` and re-downloading — but a corrupt *fresh* download has no such guard.)

**Resolution plan.**
- Broaden the Phase 2 inner guard so read/parse failures also trigger the fallback: catch `(HTTPDownloadError, ValueError, OSError, BadZipFile)` (import `BadZipFile`) around the `download_e5_data` call and set `raw_e5_df = None` on any of them, letting the existing `if raw_e5_df is None:` fallback fire. Keep `RuntimeError`/openpyxl-missing as a hard failure (a missing dependency should not silently fall back to stale data).
- Record a fallback flag in the summary (`e5_used_fallback_cache=True`) so `run_records` marks the run `recovered` (ties into A3).
- Add a test: a fresh download that reads as a corrupt workbook should resolve via the most-recent-cache path, not raise.

> [!note] Response to B2
> plan looks good

#### B3. The `Source` column is overwritten to a single literal, erasing provenance (reliability / data semantics)

**Problem.** Phase 6's `prepare_housing_output(..., source_name="DoF")` sets `result["Source"] = "DoF"` for **every** row, discarding the `E-5` / `E-8` / `Aggregated` labels that the earlier phases so carefully assigned and prioritized. The output CSV's `Source` column is therefore a constant and tells a downstream consumer nothing about whether a given place-year came from the modern E-5, the historical E-8, or a regional/state rollup. The merge still works across runs only because `load_historical_housing_data` unconditionally *re-labels* the loaded file as `E-8` — i.e. the pipeline relies on throwing the provenance away and reassigning it, which is why the `resolve_source_overlap` priority still functions despite the column being uniform on disk.

**Why it matters.** `Source` is one of the four Identity columns documented in "The Dataset." As written it is dead weight, and any analysis or QA that trusts it (e.g. "how many rows are aggregated vs. observed?") is silently wrong. It also means a future change to the merge that stops re-labeling on load would break overlap resolution in a non-obvious way.

**Resolution plan.**
- **Preserve real provenance.** Stop overwriting `Source` in `prepare_housing_output`. Let the value flow through from the phases that set it (`E-5` in cleaning, `E-8` on historical load, `Aggregated` on rollups), and drop the `source_name="DoF"` argument (or repurpose it as a dataset-level constant stored elsewhere, e.g. metadata, not per row).
- Then make the merge robust to a genuine `Source` column: `load_historical_housing_data` should keep the file's existing `Source` values instead of blanket-relabeling to `E-8`, and `resolve_source_overlap` should be given a priority list covering all observed sources (`E-5` > `E-8` > `Aggregated`, or whatever precedence you intend for overlaps).
- Add a validation assertion that `Source` is a subset of the known label set, so a stray value surfaces instead of silently passing.

> [!question] Decision for you (B3)
> Do you want per-row provenance in the output (`E-5`/`E-8`/`Aggregated`), or is `Source` meant to be a dataset-level tag ("DoF") for all rows? If the frontend or any consumer relies on `Source == "DoF"`, changing it is a breaking schema change — confirm before touching it. If nothing reads it, restoring real provenance is the higher-value choice.

> [!note] Response to B3
> Complete the resolution plan and update the front end to indicate the actual sources that the user can choose from in a multi select. Default is all for this module.

#### B4. Phase 6's missing-level pass references a `County` column that no longer exists (latent correctness)

**Problem.** The orchestrator's Phase 6 call is `assign_missing_geographic_levels(..., county_col="County", ...)`, but Phase 3 dropped both `County` and `_temp_county` via `drop_helper_columns`, and `County` is not in `output_columns`. Inside the loop, `row.get("County")` returns `None`, so every ambiguous row that reaches this pass is classified with `county_context=None` — the San Joaquin city-vs-county disambiguation (which needs `county_context == "San Joaquin"`) can never succeed here. In practice the pass rarely has work to do (all merged/enriched rows already carry a level by Phase 6), so this is latent rather than actively wrong — but it is a silent capability loss waiting for an input where a genuinely unlabeled ambiguous row survives to Phase 6.

**Resolution plan.**
- Either **remove the Phase 6 missing-level pass** if analysis confirms no row can reach it unlabeled (simplest, and it documents the invariant), **or** pass a `county_col` that actually exists at that point (there is none carrying county context in Phase 6 — so this really argues for removal), **or** if county context is genuinely needed post-merge, retain a county-context column through Phases 4-6 instead of dropping it in Phase 3.
- At minimum, add an assertion/log if `assign_missing_geographic_levels` finds any missing-level rows in Phase 6, so the "should never happen" case becomes visible rather than silently mis-handled.

> [!note] Response to B4
> plan looks good

#### B5. The decimal-fraction vacancy-rate heuristic can corrupt genuine sub-1% rates (reliability)

**Problem.** `find_decimal_fraction_rates` flags any recent (`year ≥ 2020`), non-State vacancy rate in the open band `(0.01, 1.0)` and `normalize_decimal_fraction_rates` multiplies it by 100. The intent is to fix vintages that stored `0.07` for `7%`. But a *legitimate* vacancy rate below 1% — e.g. a tiny, fully-occupied place at `0.5%` — sits in the same band and would be silently rescaled to `50%`, a gross error that then passes the 0-100 range validator. The heuristic cannot distinguish "0.5 meaning 0.5%" from "0.5 meaning 50%."

**Why it usually doesn't bite.** Sub-1% vacancy is rare in California city/county data, and the modern E-5 already stores rates as percentages, so the band is mostly empty for `year ≥ 2020`. But "rare" is not "never," and the failure is silent and directional.

**Resolution plan.**
- Tighten the signal from a per-row band to a **per-vintage** decision: for a given source/year, if the *median* (or 90th percentile) of vacancy rates is `< 1.0`, the whole vintage is fraction-encoded and every rate in it should be scaled; otherwise scale nothing. This keys off the distribution rather than individual values and cannot misfire on a lone genuine 0.5%.
- If a per-row rule must stay, cross-check against the row's own components — recompute `Vacant Units / Total Housing Units * 100` and only rescale when the stored rate ≈ that quantity / 100 — so the correction is verified against data the row already carries rather than inferred from magnitude alone.
- Add a regression test with a genuine 0.4% row that must survive unscaled.

> [!note] Response to B5
> plan looks good

#### B6. Row-by-row geographic classification is the dominant remaining CPU cost (efficiency)

**Problem.** `assign_missing_geographic_levels` iterates `for row_index in result.index[missing_level_rows]:`, materializing each row with `result.loc[row_index]` and writing back with `result.at[...]`. After the county/state promotions, the "missing" set is essentially every city and town row, so this is a Python-level loop over the bulk of the frame — the single most expensive step in the cleaning path and the one place the refactor did *not* vectorize (the doc itself notes the filters were vectorized "where the legacy code looped"; this classifier still loops). It also re-reads config per ambiguous row (A6).

**Resolution plan.**
- Vectorize the common cases and reserve the loop for true ambiguities only. The classifier is a cascade of membership tests (`State Total`/state name → State; region names → Region; `County Total` → County; town list → Town; default → City) that map cleanly to vectorized `np.select`/`Series.map` over the missing-level subset. Only `ambiguous_city_names` need per-row context, and after the county-total promotion those are a tiny minority.
- Concretely: compute the vectorized level for all missing rows via boolean masks against the config sets, then handle just the residual ambiguous rows (and only those) with whatever per-row logic A5 settles on. This turns an O(n) Python loop into O(n) vectorized work plus O(k) row work where k = ambiguous rows ≪ n.
- Do this together with A5 and A6, since all three touch the same function and call graph. Guard with the existing classification tests to prove parity.

> [!note] Response to B6
> plan looks good

#### B7. Additive-column detection is recomputed once per region (minor efficiency)

**Problem.** `build_regional_rows` calls `_aggregate_additive_columns` once per region (9 regions), and each call re-scans every column, running `pd.to_numeric(...)` and a `.notna().all()` check to decide which columns are "safely additive." The additive-column *set* is identical across regions for a given frame, so this detection is repeated ~9× per run (and again for the state rollup). The cost is small relative to the overall pandas work but is pure waste.

**Resolution plan.**
- Detect the additive column set once for the deduplicated county frame, then reuse it for every region and for the state aggregation, passing the precomputed column list into `_aggregate_additive_columns` (or splitting detection from summation into two functions). This is a localized refactor with no behavioral change; cover it with the existing aggregation tests.

> [!note] Response to B7
> plan looks good

#### B8. Cross-cutting: cold-start and wiring gaps compound (reliability)

**Problem.** A1 (self-perpetuating path), A2 (unwired Phase 0 + config that can't drive the builder), and B1 (fallback defeated by retention) individually degrade gracefully in steady state but **compound on a fresh or long-idle deployment**: a first run has no baseline (A1 cold-start abort), the operator cannot trivially build one because the Phase 0 config isn't builder-ready (A2), and if they instead lean on cached E-5 data an outage may leave nothing within the fallback window (B1). None is catastrophic alone; together they mean "stand this pipeline up on a clean machine" is not currently a documented, working path.

**Resolution plan.**
- Treat A1 + A2 as a single "baseline bootstrap" work item: separate the baseline path, ship the Phase 0 driver, and write a one-page "standing up Population & Housing from scratch" runbook (seed the baseline → verify Phase 1 validation → run the main pipeline). Then B1 stops being a silent trap because the runbook makes the retention/fallback relationship explicit.
- Add a single smoke test that runs `main()` against a minimal fixture baseline + a fixture E-5 to prove the end-to-end path works from a clean state.

> [!note] Response to B8
> plan looks good

## Legacy-to-Refactored Mapping

A one-line index from each legacy unit to where its behavior now lives.

| Legacy unit | Refactored home |
|---|---|
| `main()` monolith | `orchestrators/pophousing_pipeline.py` |
| `run_original_pipeline.py` | Replaced by the orchestrator (subprocess runner removed) |
| `cleanup_old_e5_files()` | `archives/e5_retention.py` + `shared/archives/file_retention.py` |
| `validate_historical_data()` | `validation/historical_data_validator.py` + `shared/validation/dataframe_validators.py` |
| `get_e5_file_url()`, `download_e5_data()`, `get_most_recent_e5_file()`, `get_e5_filename_from_url()` | `acquisition/dof_e5_downloader.py` + `shared/downloads/http_downloads.py` |
| `clean_e5_data()` | `cleaning/e5_pipeline.py` (orchestration) + the Phase 3 modules |
| `clean_common_e5()` | `shared/data_cleaning/row_filters.py` + `cleaning/hierarchical_location_cleaning.py` + `cleaning/location_standardization.py` |
| `add_geographic_level_e5()`, `add_geographic_level()` | `cleaning/geographic_classification.py` |
| `enhanced_forward_fill_helpers.py` | `cleaning/hierarchical_location_cleaning.py` + `cleaning/geographic_classification.py` |
| `data_cleaning_utils.py` | Split across `shared/data_cleaning/`, `shared/validation/`, `cleaning/`, and `calculations/` |
| `standardize_city_names()` | `cleaning/location_standardization.py` |
| `standardize_san_francisco_classification()` | `cleaning/geographic_classification.py` |
| Phase 4 inline merge | `merging/historical_modern_merge.py` |
| `add_regional_data()` | `aggregation/regional_aggregation.py` + `aggregation/aggregation_utils.py` |
| `add_state_data_for_modern_years()` | `aggregation/state_aggregation.py` |
| `fix_vacancy_rate_decimal_fractions()` | `calculations/rate_normalization.py` + `validation/aggregation_validators.py` |
| Housing formulas and rate math | `calculations/housing_metrics.py` |
| Phase 6 finalize and write | `output/finalize_dataset.py` + `validation/final_dataset_validator.py` |
| `download_historical_data.py` | `acquisition/dof_historical_downloader.py` + `shared/downloads/http_downloads.py` |
| `historical_data_processor.py` | The seven `historical/` modules |
| `logging_config.py` | `shared/logging/` (`pipeline_logging.py` + `dataframe_logging.py` + `run_records.py`, fully implemented — see A3) |
| `pophousing_config.py` | `lib/pophousing_config.py` + the four `config/` accessor modules + `shared/geography/california_geography.py` |

## Open Items

Each item links to the fuller problem statement and plan in "Flagged Issues and Fragilities." All backend items are implemented with tests as of 2026-07-11; the two frontend deliverables remain.

**Reliability / robustness**
- [x] **(A1)** Historical baseline given its own immutable path (`PopHousing_Historical_E8.csv`), decoupled from `PopHousing_Current.csv`; cold start now raises an actionable "run Phase 0" error. *Decision: committed, versioned artifact with a manual Phase 0 rebuild.*
- [x] **(A2 + B8)** `historical/build_baseline.py` ships as the Phase 0 driver (maps `HISTORICAL_FILE_CONFIG` into the builder's shape, `--from-current` seed + workbook rebuild), with a freshness check, the [[pophousing-standup]] runbook, and a from-clean-state smoke test. *Decision: manual + freshness warning; a frontend button will trigger it.*
- [x] **(B1)** Cache/retention window set to 90 days, fallback to 400 days, and `get_most_recent_e5_file` now searches the archive as well, so an archived-out workbook is reachable during an outage.
- [x] **(B2)** Phase 2 guard broadened to catch read/parse failures on a fresh download (falling back to cache), while a missing `openpyxl` still fails hard; degraded runs flag as `recovered`.
- [x] **(B3)** `Source` provenance preserved to the output (`E-5`/`E-8`/`Aggregated`), `Source` is a required column, and the final validator gates on known sources. *Decision: per-row provenance; a frontend multi-select (default all) is the remaining follow-up.*
- [x] **(B4)** The broken Phase 6 missing-level pass was removed and replaced with a loud invariant assertion.
- [x] **(B5)** The decimal-fraction heuristic is now a per-vintage (median-based) rule, so a genuine sub-1% rate is never rescaled.

**Correctness housekeeping**
- [x] **(A5)** The ambiguous-location lookahead was removed (redundant after county/state promotion); its rebuild recipe is documented in the function description and code comment.
- [x] **(A4)** `has_meaningful_housing_data` removed as dead parity code.

**Efficiency**
- [x] **(B6 + A6)** The geographic-classification loop is vectorized for the unambiguous cascade (per-row work reserved for true ambiguities), and the geography config is built once and threaded through the hot path.
- [x] **(B7)** The additive-column set is detected once on the county frame and reused across all regions.

**Documentation**
- [x] Corrected the stale "unimplemented stubs" language in the `shared/logging/` body text.
- [x] Filled the Phase 2/4/5 structured-logging coverage gaps (`main` now emits `log_processing_step` at every phase boundary).

**Frontend follow-ups (tracked separately)**
- [ ] **(A2)** "Update data" button so a researcher can trigger the Phase 0 rebuild without touching the codebase.
- [ ] **(B3)** `Source` multi-select in the module UI (default: all), reflecting the real provenance values now in the output.

**Resolved before this pass**
- [x] **(A3)** `shared/logging/` (`pipeline_logging.py`, `dataframe_logging.py`, `run_records.py`) fully implemented and wired through `execute_pipeline_run`.
